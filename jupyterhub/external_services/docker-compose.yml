version: '3.8'

services:
  test:
    build: "test"
    networks:
      - jupyterhub_step1
    environment:
      JUPYTERHUB_SERVICE_PREFIX: "/services/external-dashboards-test/"

networks:
  jupyterhub_step1:
    driver: bridge
    name: net_basic

